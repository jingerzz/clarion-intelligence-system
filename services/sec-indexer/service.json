{
  "label": "sec-indexer",
  "mode": "process",
  "entrypoint": "sec-indexer",
  "workdir": "/home/workspace",
  "env_vars": {
    "SEC_USER_AGENT": "$SEC_USER_AGENT"
  },
  "description": "Background indexer for SEC EDGAR filings. Watches ~/clarion/queue/ and runs the fetch \u2192 extract \u2192 tree \u2192 save pipeline. Idempotent on filing accession.",
  "_notes": {
    "env_vars_syntax": "Values like '$ZO_API_KEY' tell Zo to resolve the named secret (Settings \u2192 Advanced \u2192 Secrets) at service start. SEC_USER_AGENT is optional \u2014 if no secret of that name exists, the service falls back to the lib's DEFAULT_USER_AGENT.",
    "param_names": "Use 'workdir' and 'env_vars' (snake_case) when calling register_user_service \u2014 these are the canonical keys Zo's service tool accepts."
  }
}
